struct Point {
  x: i32,
  y: i32
}

fn make_point(a: i32, b: i32) returns Point {
  return Point { x: a, y: b }
}

fn sum_point(p: Point) returns i32 {
  return p.x + p.y
}

fn main() returns i32 {
  let p: Point = make_point(7, 5)
  return sum_point(p)
}
