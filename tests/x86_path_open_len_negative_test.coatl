fn main() -> i32 {
  let fd_ptr: i32 = 4096;
  let path_ptr: i32 = "probe_out.txt";
  let bad_len: i32 = 0 - 1;
  let rc: i32 = __path_open(3, 0, path_ptr, bad_len, 1, 0, 0, 0, fd_ptr);
  return rc;
}
