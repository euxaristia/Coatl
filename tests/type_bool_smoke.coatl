fn check(flag: bool) -> i32 {
  if (flag) {
    return 1;
  }
  return 0;
}

fn main() -> i32 {
  let a: bool = true;
  let b: bool = false;
  let r1: i32 = check(a);
  let r2: i32 = check(b);
  return r1 * 10 + r2;
}
